[advice]
    skippedCherryPicks = false
[alias]
    amend = commit --amend --no-edit
    autosquash = !GIT_SEQUENCE_EDITOR=: git rebase -i --autosquash
    diffc = diff --cached
    fixup = commit --fixup
    fixupnow = !bash -c \"git fixup "${1}" && git autosquash "${1}~1"\"
    ifixup = !git commit --fixup "$(git isha)"
    ifixupnow = !git fixupnow "$(git isha)"
    plog = log --graph --abbrev-commit --date=relative --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)'
    merged = !git branch --merged "$(git obranch)" | grep -v "$(git obranch)"
    branchr = branch --sort=-committerdate --format=\"%(committerdate:relative)%09%(refname:short)\"

    # get the default branch name from the origin
    obranch = !bash -c \"(git cat-file -e origin/HEAD 2>/dev/null || git remote set-head origin --auto >/dev/null) && git rev-parse --abbrev-ref origin/HEAD | cut -b8-\"

    # Interactively pick commit sha(s)
    isha = !git rev-parse && git log --pretty=oneline | fzf --with-nth 2.. --multi --no-sort --preview='git show --color=always {1}' | cut -d' ' -f1
[branch]
    sort = -committerdate  # sort branches most recent commit first
[color "status"]
    untracked = cyan
[commit]
    verbose = true  # Show the current diff in the commit message template
[diff]
    algorithm = histogram  # Slightly slower for large diffs, but generally more readable
    submodule = log  # Show a log of added/removed commits for a changed submodule
    colorMoved = default  # mark up moved code differently than added/removed
    renames = true  # detect if a file has been renamed
    mnemonicPrefix = true  # use [i]ndex, [w]orking dir, [c]ommit in diff headers instead of a/b
    context = 10  # add more context in diffs
[diff "exif"]  # add a exif info diff handler
    binary = true
    textconv = exiftool
[fetch]
    prune = true
    pruneTags = true
[filter "lfs"]
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
[init]
    defaultBranch = master
[log]
    follow = true  # Assume --follow when given a single path
[merge]
    # Use a diff3-styled vimdiff
    guitool = gvimdiff3
    tool = vimdiff3
    conflictStyle = zdiff3
[mergetool]
    prompt = false
[pull]
    ff = only  # Don't merge or rebase by default when pulling
[push]
    # keep default push mode, but automatically set up tracking
    default = simple
    autoSetupRemote = true
[rebase]
    autoSquash = true
    autoStash = true
    updateRefs = true  # auto-update refs when rebasing - good for stacked branches
    missingCommitsCheck = warn
[rerere]
    autoUpdate = true
    enabled = true
[stash]
    showPatch = true
[status]
    submoduleSummary = true
[submodule]
    recurse = true
[tag]
    sort = version:refname  # sort tags as versions
[transfer]
    fsckObjects = true  # detect data corruption
[user]
    email = carey@cmetcalfe.ca
    name = Carey Metcalfe
